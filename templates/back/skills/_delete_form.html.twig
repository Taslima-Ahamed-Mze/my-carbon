<form method="post" action="{{ path('back_app_skills_delete', {'id': skills.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ skills.id) }}">
    <button class="btn">Delete</button>
</form>
