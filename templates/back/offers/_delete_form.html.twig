<form method="post" action="{{ path('back_app_offers_delete', {'id': offer.id}) }}" onsubmit="return confirm('Es-tu sûr de vouloir supprimer cet offre?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offer.id) }}">
    <button class="btn devisio-btn-delete">Supprimer</button>
</form>
