<form method="post" action="{{ path('back_app_event_delete', {'id': event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event.id) }}">
	<button class="btn">Supprimer
		<i class="fa-solid fa-trash fa-2xl" style="color: #E53F49"></i>
	</button>
</form>

{# <div>
	<a href="{{ path('back_app_event_delete', {'id': event.id})}}">Supprimer

	</a>
</div> #}
