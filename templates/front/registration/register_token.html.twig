{% extends 'base.html.twig' %}

{% block title %}
  Inscription
{% endblock %}

{% block body %}
  <div class="form-wrapper">
    {{ form_start(registrationTokenForm) }}
    <h1>
      Veuillez renseigner le code reçu par email
    </h1>
    {% if error is defined %}
      <li class="register-error mt-5" style="display: block;">{{ error }}</li>
    {% endif %}
    {{ form_widget(registrationTokenForm) }}
    <button type="submit" class="devisio-btn">Valider</button>
    <input type="hidden"
      name="_csrf_token"
      value="{{ csrf_token('authenticate') }}" />
    {{ form_end(registrationTokenForm) }}
  </div>
{% endblock %}
