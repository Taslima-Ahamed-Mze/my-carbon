<form method="post" action="{{ path('app_cooptation_steps_delete', {'id': cooptation_step.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cooptation_step.id) }}">
    <button class="btn">Delete</button>
</form>
